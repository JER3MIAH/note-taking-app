import 'package:flutter/material.dart';

class FontThemeScreen extends StatelessWidget {
  const FontThemeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}