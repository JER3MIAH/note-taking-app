import 'package:flutter/material.dart';

class DesktopAllNotesView extends StatelessWidget {
  const DesktopAllNotesView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}